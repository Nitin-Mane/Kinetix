//! std::map — Map operations for Kinetix (stub for Phase 1).

use kinetix_vm::vm::Vm;

/// Register map module — Phase 1 stub.
pub fn register(_vm: &mut Vm) {
    // Map operations will be registered in Phase 1e.
    // The Value::Map variant and KxMap runtime type are defined in kinetix_vm.
}
